import { PscCurrenyCustomPipePipe } from './psc-curreny-custom-pipe.pipe';

describe('PscCurrenyCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PscCurrenyCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
